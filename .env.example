# Copy to .env locally. On Railway, set in Variables (never commit .env).

FLOAT_API_KEY=
CSGOEMPIRE_API_KEY=
# Optional: search falls back to PricEmpire when Skinport has no match. Skinport catalog needs no key.
PRICEMPIRE_API_KEY=
STEAM_API_KEY=
